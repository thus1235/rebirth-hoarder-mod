# -*- coding: utf-8 -*-
"""在使用说明.txt 末尾追加「改了没反应」排查章节（UTF-8 BOM 文件，追加时不重复写 BOM）。"""
import io

doc = r"D:\桌面\末世房车MOD工具库\修改器\使用说明.txt"

section = """

────────────────────────────────────────────────
【三】存档修改器「写入成功，但游戏里没变」怎么办
────────────────────────────────────────────────

先跑一键诊断：双击本文件夹里的「诊断存档问题.bat」
  → 会自动生成并打开「诊断报告.txt」，把这份报告发给作者即可定位。
  （诊断只读，不会改任何游戏文件，可放心运行）

自己也可以按下面 5 条顺序自查，命中率从高到低：

 ① 改错目录 / 改错槽位（最常见）
    修改器顶部默认停在 progress-current.json。
    · 如果你在游戏里读的是「手动存档1/2/3」，修改器顶部「槽位」也要切到对应档；
    · 如果游戏存档不在 %APPDATA%\\Rebirth Hoarder\\steam-cloud\\saves，
      点顶部「浏览…」指向真正的 saves 文件夹再读取。
    诊断报告的「3. 存档体检」会直接告诉你游戏最近在用哪个档。

 ② 游戏没关干净（红色标签页必看）
    当前局 / P2战斗 / 物品 / 角色属性 / 装备 / 命途 / 医疗舱 是「🔒 局外可改」，
    游戏运行中会被自动保存覆盖。必须：完全退出游戏 → 改 → 写入 → 再启动游戏。
    注意 Steam 版关掉窗口后进程可能残留，任务管理器里确认 Rebirth Hoarder 已消失。

 ③ Steam 云同步开着（仅 Steam 正版）
    一开游戏，云端旧档会被拉回来覆盖本地修改。
    解决：Steam → 库 → 右键游戏 → 属性 → 通用 → 取消「保持此游戏的跨设备同步」；
    或弹「存档冲突」时选「使用本地并上传」。

 ④ 游戏版本和本修改器对不上
    游戏会校验存档，校验不通过的档会被丢进 saves\\corrupt\\ 并回退旧档 ——
    表现就是「改了没反应」。诊断报告的「2. 游戏目录 / 版本」会给出 app.asar 字节数，
    「6. 游戏是否把存档判为损坏」会给出铁证。这需要对新版本重新适配，把报告发给作者。

 ⑤ 环境问题
    双击 exe 没反应 / 闪退：多半缺 .NET Framework 4.8（诊断报告第 0 节会提示）；
    被 Windows SmartScreen 拦：右键 exe → 属性 → 勾选「解除锁定」；
    被杀软误删：把整个文件夹加入白名单后重新解压。
"""

with io.open(doc, "a", encoding="utf-8") as f:
    f.write(section)
print("appended")
